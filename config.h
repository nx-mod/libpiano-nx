#pragma once

/* guarded so a consumer's own config.h (e.g. pianobar-nx's) can define
 * these first without a redefinition warning */
#ifndef PACKAGE
#define PACKAGE "libpiano-nx"
#endif
#ifndef VERSION
#define VERSION "nx-1"
#endif
